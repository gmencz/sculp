describe("smoke tests", () => {
  afterEach(() => {
    cy.cleanupUser();
  });

  it("should allow you to sign in", () => {
    // todo
  });

  it("should allow you to create an account", () => {
    // todo
  });
});
